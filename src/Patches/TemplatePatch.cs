using HarmonyLib;
using flanne;
using UnityEngine;

namespace DebugUtilityMod
{
    //[HarmonyPatch]
    class TemplatePatch
    {
        /*
        [HarmonyPatch(typeof(), "")]
        [HarmonyPrefix]
        static void NAME()
        {
            Debug.Log("Entered in prefix");
        }
        */
    }
}
